# AXIS Studio - Текущие Задачи

Этот файл отслеживает задачи, над которыми мы работаем в данный момент.

## Задача: Переход на безопасный Gemini API и генерация темы

**Статус:** В процессе

### Выполненные подзадачи:
- [x] Создан новый провайдер `gemini_api_provider.py` для прямого доступа к API.
- [x] Реализована логика `GeminiApiProvider` с использованием официальной библиотеки Google.
- [x] Новый провайдер `gemini_api` зарегистрирован в системе.
- [x] `gemini_api` сделан провайдером по умолчанию вместо старого `gemini_browser`.
- [x] Добавлен ограничитель запросов (8 RPM) для контроля расходов.
- [x] Предоставлена инструкция по безопасной аутентификации через `gcloud`.

### Текущая задача:
- [ ] **(Ожидает вас)** Настроить бюджет и оповещения в Google Cloud для контроля расходов.

### Следующие шаги:
- [ ] Запустить генерацию темы с использованием нового, аутентифицированного `gemini_api` провайдера.
