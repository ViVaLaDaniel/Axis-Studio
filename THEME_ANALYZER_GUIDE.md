# Theme Analyzer — Quick Start Guide

## Зачем нужна база знаний?

### ❌ Без базы знаний:
```
Генератор → LLM → "Создай hero section"
LLM: *генерирует из воздуха*
Результат: generic код, может не работать, нет best practices
```

### ✅ С базой знаний:
```
Генератор → LLM + База (Dawn, Impact знания) → "Создай hero section как в Dawn, но с video из Impact"
LLM: *использует реальные рабочие примеры*
Результат: профессиональный код, проверенные паттерны, стабильное качество
```

**База знаний = рецепты для LLM. Без рецептов — результат случайный.**

---

## Установка

Инструмент уже готов:
```
src/utils/theme_analyzer.py
```

Зависимости: `git` (для скачивания тем)

---

## Использование

### 1. Анализ одной темы (GitHub)

```bash
python src/utils/theme_analyzer.py --theme dawn --github https://github.com/Shopify/dawn
```

**Что происходит:**
1. Скачивает Dawn с GitHub
2. Парсит структуру (sections, snippets, assets)
3. Извлекает settings_schema.json
4. Анализирует компоненты (product-card, price)
5. Определяет UX-паттерны (sticky header, drawer cart)
6. Измеряет производительность (размер CSS/JS)
7. Генерирует `00_CORE_BRAIN/THEME_DB_DAWN.json`

### 2. Анализ локальной темы

```bash
python src/utils/theme_analyzer.py --theme myTheme --path C:/path/to/theme
```

### 3. Batch Analysis (все топ-темы)

Создай файл `analyze_top10.bat`:
```batch
@echo off
echo Starting batch analysis of top 10 Shopify themes...

REM Dawn (Free, Shopify)
python src/utils/theme_analyzer.py --theme dawn --github https://github.com/Shopify/dawn

REM Impact (если есть доступ к репо)
REM python src/utils/theme_analyzer.py --theme impact --path C:/path/to/impact

REM Prestige
REM python src/utils/theme_analyzer.py --theme prestige --path C:/path/to/prestige

echo Done! Check 00_CORE_BRAIN/ for THEME_DB_*.json files
pause
```

---

## Результат

После анализа Dawn:
```
00_CORE_BRAIN/
└── THEME_DB_DAWN.json  ← Полная декомпозиция
    ├── metadata (name, developer, price)
    ├── file_structure (50 sections, 25 snippets, bundle sizes)
    ├── sections_library (header, hero, product page с schemas)
    ├── components_library (product-card, price)
    ├── ux_patterns (sticky header, drawer cart)
    ├── settings (120 total, categories)
    └── performance (Lighthouse 95+, LCP 1.0s)
```

---

## Ограничения текущей версии

### ✅ Работает:
- Анализ структуры (файлы, директории)
- Извлечение схем из Liquid
- Подсчёт настроек (settings_schema)
- Измерение размера бандлов (CSS/JS)
- Определение UX-паттернов (по названиям файлов)

### ⚠️ Требует ручной доработки:
- **Детальное описание компонентов** — анализатор создаёт базовую структуру, но описания нужно дополнить вручную (как я сделал для Dawn)
- **CRO фичи** — автоматически не определяет (low stock, urgency), нужно добавлять вручную
- **Accessibility audit** — нужен ручной анализ
- **Performance benchmarks** — Lighthouse нужен отдельный запуск

### 🚀 Будущие улучшения:
- Интеграция Lighthouse API (автоматический performance audit)
- AI-анализ Liquid кода (понимание логики компонентов)
- Автоматическое определение CRO паттернов
- Мониторинг новых тем (webhook от Shopify Theme Store)

---

## Workflow: как пополнять базу

### 1. Автоматический анализ (быстро)
```bash
python src/utils/theme_analyzer.py --theme impact --github https://github.com/.../impact
```
Результат: базовая структура за 10 секунд

### 2. Ручное дополнение (качество)
Открой созданный `THEME_DB_IMPACT.json` и дополни:
- Детальные описания секций
- UX-паттерны (behaviour, triggers)
- CRO фичи
- Accessibility notes
- AXIS_LEARNING_POINTS

**Рекомендация:** 80% автоматически + 20% ручная доработка = идеальная база

---

## Приоритет анализа

### Tier 1 (обязательно):
1. ✅ **Dawn** — сделан вручную (эталон)
2. ✅ **Impact** — сделан вручную (fashion)
3. ⏳ **Prestige** — запустить анализатор + доработка
4. ⏳ **Impulse** — запустить анализатор

### Tier 2 (опционально):
5. Focal (high-volume stores)
6. Motion (creative brands)
7. Refresh (wellness)

### Tier 3 (low priority):
8-10. Warehouse, Expanse, Symmetry

**После 4-х тем (Dawn, Impact, Prestige, Impulse) — можно начинать production генерацию!**

---

## Тестирование базы знаний

После добавления тем, протестируй:

```bash
python src/cli/main.py create-theme "VIP тема для люксовых часов"
```

Проверь что генератор:
- ✅ Загружает данные из базы (Dawn, Impact, Prestige)
- ✅ Упоминает best practices в промптах
- ✅ Генерирует секции на основе примеров
- ✅ CRO паттерны соответствуют нише

---

## Следующий шаг

**Рекомендую:**
1. Запустить анализатор для **Prestige** (luxury theme)
2. Дополнить результат вручную (20% доработки)
3. Запустить тестовую генерацию темы
4. Если качество ОК → продолжить с Impulse

**Готов начать?** Скажи и я помогу запустить анализ Prestige!
